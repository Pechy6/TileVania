using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class fungujeto : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        debug.Debug.Log("funguje to?");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
